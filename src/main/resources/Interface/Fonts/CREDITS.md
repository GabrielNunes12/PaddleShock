# Font credits

- `Default.fnt` / `Default.png` — bitmap-rasterized from **Anton** by The Anton Project
  Authors, [SIL Open Font License 1.1](https://openfontlicense.org/), via
  [Google Fonts](https://fonts.google.com/specimen/Anton). Replaces jME3's built-in default
  font project-wide (Lemur's `GuiGlobals` and all in-house `loadFont(...)` calls resolve
  `Interface/Fonts/Default.fnt` here first, ahead of the copy bundled in `jme3-core.jar`).
  Full license text: `Anton-OFL.txt` in this folder.
